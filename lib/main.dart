import 'package:flutter/material.dart';
import 'messages.dart';
import 'chat.dart';

void main() => runApp(
      MaterialApp(
        home: Scaffold(
          body: Messages(),
        ),
      ),
    );
